{
  "name": "triggrpay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently --kill-others-on-fail false -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "clean:client": "node client/scripts/clean-vite-cache.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
